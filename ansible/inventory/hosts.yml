all:
  children:
    pi5:
      hosts:
        rpi5:
          ansible_host: 192.168.2.11
          ansible_user: ananchev
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_pi5
    # macmini:   # added later (Fedora Asahi) — same RHEL-family path
    #   hosts:
    #     macmini:
    #       ansible_host: ...
